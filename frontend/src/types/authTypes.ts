export interface LoginData{
    email:string;
    password:string;
}

export interface CadastroData{
  name: string;
  email: string;
  password: string;
}

export interface ListaIdProps{
  description:string;
  status:string;
  scheduleAt:string;
}